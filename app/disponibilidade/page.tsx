"use client";
import {useState} from "react";import {CheckCircle2,Search,XCircle} from "lucide-react";import {SiteHeader} from "@/components/site-header";import {SiteFooter} from "@/components/site-footer";
export default function DisponibilidadePage(){const[result,setResult]=useState<null|boolean>(null);return <><SiteHeader/><main className="container-page py-12"><div className="mx-auto max-w-2xl"><h1 className="text-4xl font-black uppercase">Consulte a <span className="text-brand-red">disponibilidade</span></h1><p className="mt-3 text-zinc-400">Verificação demonstrativa usando estado local.</p><div className="card mt-8 grid gap-4 p-6"><input className="input-dark" type="date"/><input className="input-dark" placeholder="Ex: São Paulo - SP"/><button onClick={()=>setTimeout(()=>setResult(Math.random()>.25),350)} className="flex items-center justify-center gap-2 rounded-xl bg-brand-red px-5 py-4 font-bold"><Search size={18}/> Verificar disponibilidade</button></div>{result!==null&&<div className={`card mt-4 flex items-start gap-3 p-5 ${result?"border-emerald-500/50":"border-red-500/50"}`}>{result?<CheckCircle2 className="text-emerald-400"/>:<XCircle className="text-red-400"/>}<div><strong className={result?"text-emerald-400":"text-red-400"}>{result?"DATA DISPONÍVEL!":"DATA INDISPONÍVEL"}</strong><p className="mt-1 text-sm text-zinc-400">{result?"Você já pode solicitar um orçamento.":"Escolha outra data e tente novamente."}</p></div></div>}</div></main><SiteFooter/></>}