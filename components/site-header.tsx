"use client";
import Link from "next/link";import {Menu,Instagram,Youtube,Music2} from "lucide-react";import {Logo} from "./logo";import {useState} from "react";
const links=[["Início","/"],["Sobre","/#sobre"],["Agenda","/agenda"],["Fotos e Vídeos","/#midia"],["Contato","/contratacao"]];
export function SiteHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-zinc-800 bg-black/90 backdrop-blur"><div className="container-page flex min-h-16 items-center justify-between gap-6"><Link href="/"><Logo compact/></Link><nav className="hidden items-center gap-7 text-xs font-bold uppercase tracking-[0.18em] md:flex">{links.map(([label,href])=><Link key={label} href={href} className="text-zinc-300 transition hover:text-brand-red">{label}</Link>)}</nav><div className="hidden items-center gap-4 md:flex"><Instagram size={18}/><Youtube size={19}/><Music2 size={18}/></div><button className="md:hidden" onClick={()=>setOpen(v=>!v)} aria-label="Abrir menu"><Menu/></button></div>{open&&<div className="border-t border-zinc-900 bg-black md:hidden"><div className="container-page grid gap-3 py-4 text-sm font-semibold">{links.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</div></div>}</header>}