"use client";
import Link from "next/link";import {Menu,Instagram,Youtube,Music2} from "lucide-react";import {Logo} from "./logo";import {useState} from "react";
const links=[["Início","/"],["Sobre","/#sobre"],["Agenda","/agenda"],["Fotos e Vídeos","/#midia"],["Contato","/contratacao"]];
export function SiteHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-zinc-800 bg-black/95"><div className="container-page flex min-h-[76px] items-center justify-between gap-6"><Link href="/"><Logo compact/></Link><nav className="hidden items-center gap-8 text-xs font-bold uppercase tracking-[.12em] md:flex">{links.map(([label,href],i)=><Link key={label} href={href} className={i===0?"border-b-2 border-brand-red py-7 text-brand-red":"py-7 text-zinc-200 transition hover:text-brand-red"}>{label}</Link>)}</nav><div className="hidden items-center gap-5 md:flex"><Instagram size={20}/><Music2 size={20}/><Youtube size={21}/><span className="grid h-5 w-5 place-items-center rounded-full border text-[9px] font-black">S</span><Menu size={22}/></div><button className="md:hidden" onClick={()=>setOpen(v=>!v)} aria-label="Abrir menu"><Menu size={27}/></button></div>{open&&<div className="border-t border-zinc-900 bg-black md:hidden"><div className="container-page grid gap-4 py-5 text-sm font-bold uppercase">{links.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</div></div>}</header>} 