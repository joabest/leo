import {Clock} from "lucide-react";import type {Show} from "@/mock/shows";
export function ShowCard({show}:{show:Show}){const available=show.status==="Disponível";return <article className="card flex items-center gap-4 p-4"><div className="grid h-16 w-16 shrink-0 place-items-center rounded-xl bg-brand-red text-center font-black leading-none"><div><div className="text-2xl">{show.day}</div><div className="text-xs">{show.month}</div></div></div><div className="min-w-0 flex-1"><h3 className="font-bold">{show.city}</h3><p className="truncate text-sm text-zinc-400">{show.venue}</p><p className="mt-1 flex items-center gap-1 text-xs text-zinc-500"><Clock size={13}/>{show.time}</p></div><span className={`rounded-full border px-3 py-1 text-[11px] font-bold uppercase ${available?"border-emerald-500/50 text-emerald-400":"border-red-500/50 text-red-400"}`}>{show.status}</span></article>}